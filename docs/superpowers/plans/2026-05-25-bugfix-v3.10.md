# Bug Fix v3.10 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修復 5 個已確認的 Bug，讓插件在任何 Figma 專案與任何試算表格式下都不會 crash 或靜默失敗。

**Architecture:** 只改兩個檔案：`figma-plugin/ui.html`（CSS class 名稱）和 `figma-plugin/code.js`（執行時防護）。所有修改都是精準的點對點修正，不改任何功能邏輯。

**Tech Stack:** Figma Plugin API（JS sandbox，ES6+ 可用，不支援 `?.`）、HTML/CSS（ui iframe）

---

### Task 1：備份 + CSS class 名稱修正（Bug ①，ui.html）

**Files:**
- Backup: `figma-plugin/code.js` → `figma-plugin/code_v3.9_pre.js`（不 commit，.gitignore 已排除）
- Modify: `figma-plugin/ui.html`（3 處字串）

- [ ] **Step 1：備份 code.js**

  執行：
  ```powershell
  Copy-Item "figma-plugin\code.js" "figma-plugin\code_v3.9_pre.js"
  ```

- [ ] **Step 2：修正 `showStyleStatus` 的 class 名稱**

  在 `figma-plugin/ui.html` 找到（約第 529–533 行）：
  ```js
  function showStyleStatus(type, text) {
    const box = document.getElementById('styleStatusBox');
    box.className = 'status-box status-' + type;
    box.textContent = text;
  }
  ```
  改為：
  ```js
  function showStyleStatus(type, text) {
    const box = document.getElementById('styleStatusBox');
    box.className = 'status-box ' + type;
    box.textContent = text;
  }
  ```

- [ ] **Step 3：修正 `bullet-done` handler 的 class 名稱**

  在 `figma-plugin/ui.html` 找到（約第 482–486 行）：
  ```js
      case 'bullet-done':
        document.getElementById('bulletStatusBox').className = 'status-box status-done';
        document.getElementById('bulletStatusBox').textContent = msg.text;
        document.getElementById('btnFixBullet').disabled = false;
        break;
  ```
  改為：
  ```js
      case 'bullet-done':
        document.getElementById('bulletStatusBox').className = 'status-box done';
        document.getElementById('bulletStatusBox').textContent = msg.text;
        document.getElementById('btnFixBullet').disabled = false;
        break;
  ```

- [ ] **Step 4：修正 `bullet-error` handler 的 class 名稱**

  在 `figma-plugin/ui.html` 找到（約第 487–491 行）：
  ```js
      case 'bullet-error':
        document.getElementById('bulletStatusBox').className = 'status-box status-error';
        document.getElementById('bulletStatusBox').textContent = msg.text;
        document.getElementById('btnFixBullet').disabled = false;
        break;
  ```
  改為：
  ```js
      case 'bullet-error':
        document.getElementById('bulletStatusBox').className = 'status-box error';
        document.getElementById('bulletStatusBox').textContent = msg.text;
        document.getElementById('btnFixBullet').disabled = false;
        break;
  ```

- [ ] **Step 5：Figma 內驗證**

  1. 在 Figma 載入插件（Import plugin from manifest）
  2. 選取任意一個文字節點（或 Frame）
  3. 展開「⑦ 後處理工具：修正圓點字型」→ 點「修正圓點字型 → Noto Sans TC」
  4. ✅ 預期：出現**綠色**「✅ 完成！共修正 N 個圓點符號 → Noto Sans TC」
  5. 展開「⑥ 後處理工具：指定字元換字型」→ 輸入字元和字型 → 點「套用」
  6. ✅ 預期：出現對應的**綠色**或**紅色**狀態框
  7. ❌ 之前行為：狀態框完全空白，沒有任何顯示

- [ ] **Step 6：Commit**

  ```bash
  git add figma-plugin/ui.html
  git commit -m "fix(ui): 修正 style/bullet 狀態框 CSS class 名稱錯誤（Bug ①）"
  ```

---

### Task 2：修正 handleIconsOnly Instance 全體中斷（Bug ②，code.js）

**Files:**
- Modify: `figma-plugin/code.js`（`handleIconsOnly` 函式）

- [ ] **Step 1：在 `iconCount` 宣告前加入 `skippedCount` 計數器**

  找到（約第 383 行）：
  ```js
  let iconCount = 0;
  for (const textNode of textNodes) {
  ```
  改為：
  ```js
  let iconCount = 0;
  let skippedCount = 0;
  for (const textNode of textNodes) {
  ```

- [ ] **Step 2：將 `return` 改為 `skippedCount++; continue;`**

  找到（約第 384–390 行）：
  ```js
    let anc = textNode.parent;
    while (anc && anc.type !== 'INSTANCE') anc = anc.parent;
    if (anc) {
      figma.ui.postMessage({ type: 'icon-error', text: '⚠️ 偵測到文字在元件實例內，請先 Detach Instance' });
      return;
    }
  ```
  改為：
  ```js
    let anc = textNode.parent;
    while (anc && anc.type !== 'INSTANCE') anc = anc.parent;
    if (anc) {
      skippedCount++;
      continue;
    }
  ```

- [ ] **Step 3：在 `icon-done` 訊息附加跳過統計**

  找到（約第 560 行）：
  ```js
  figma.ui.postMessage({ type: 'icon-done', text: `🎯 完成！共插入 ${iconCount} 個圖示` });
  ```
  改為：
  ```js
  let doneText = `🎯 完成！共插入 ${iconCount} 個圖示`;
  if (skippedCount > 0) doneText += `\n⚠️ ${skippedCount} 個節點在元件實例內已跳過（請先 Detach Instance）`;
  figma.ui.postMessage({ type: 'icon-done', text: doneText });
  ```

- [ ] **Step 4：Figma 內驗證**

  1. 建立一個 Component（內含文字 `[TAG]`），放一個 Instance 在畫布上
  2. 同時選取：這個 Instance **和** 一個正常的文字節點（也含 `[TAG]`）
  3. 確認畫布上有名為 `TAG` 的 Component
  4. 點「換圖」
  5. ✅ 預期：正常文字節點被處理，Instance 跳過，最後顯示「⚠️ 1 個節點在元件實例內已跳過」
  6. ❌ 之前行為：遇到 Instance 就整個中斷，正常節點也無法處理

- [ ] **Step 5：Commit**

  ```bash
  git add figma-plugin/code.js
  git commit -m "fix: handleIconsOnly 遇 Instance 改為跳過繼續，不再中斷全部（Bug ②）"
  ```

---

### Task 3：修正混合字型 textNode 未防護（Bug ③，code.js）

**Files:**
- Modify: `figma-plugin/code.js`（`handleIconsOnly` 函式，Task 2 之後的版本）

- [ ] **Step 1：把單一的 `ensureFont(textNode.fontName)` 改為安全呼叫**

  在 `handleIconsOnly` 中，找到 Task 2 修改後 `continue` 那一段的下方（約第 392 行），原本是：
  ```js
    await ensureFont(textNode.fontName);
  ```
  改為：
  ```js
    if (typeof textNode.fontName === 'symbol') {
      // 混合字型：逐段載入每個 segment 使用的字型
      try {
        const fontSegs = textNode.getStyledTextSegments(['fontName']);
        for (let si = 0; si < fontSegs.length; si++) {
          if (typeof fontSegs[si].fontName !== 'symbol') {
            await ensureFont(fontSegs[si].fontName);
          }
        }
      } catch (_) {}
    } else {
      await ensureFont(textNode.fontName);
    }
  ```

- [ ] **Step 2：Figma 內驗證**

  1. 建立一個文字節點，內容為「Hello `[TAG]` World」
  2. 選取其中一個字，改成不同字型（例如把「Hello」改成 Bold）→ 這樣 fontName 就是 mixed
  3. 選取這個文字節點，點「換圖」
  4. ✅ 預期：正常執行，不 crash，`[TAG]` 被替換
  5. ❌ 之前行為：`figma.loadFontAsync(Symbol)` 丟出例外，整個換圖中斷

- [ ] **Step 3：Commit**

  ```bash
  git add figma-plugin/code.js
  git commit -m "fix: 混合字型 textNode fontName 防護，避免 crash（Bug ③）"
  ```

---

### Task 4：修正硬編碼欄位索引（Bug ④，code.js）

**Files:**
- Modify: `figma-plugin/code.js`
  - `findColumns` 函式：新增 `contentSch` / `imageSch` 偵測
  - `findPrimaryRuleText` 函式：接收 `colIndex` 第三參數，改用動態索引
  - `detectTableInfo` 函式：接收 `colIndex` 第三參數，改用動態索引
  - `generateManual` 函式：更新兩個呼叫點

- [ ] **Step 1：在 `findColumns` 新增 `contentSch` 和 `imageSch`**

  找到 `findColumns` 內的 `const idx = { ... }` 物件（約第 236–243 行）：
  ```js
    const idx = {
      titleSch: find(["標題sch", "标题sch"]),
      ruleSch:  find(["規則sch", "规则sch"]),
      titleEN:  find(["標題en", "标题en", "titleen"]),
      ruleEN:   find(["規則en", "规则en", "ruleen"]),
      tableSch: find(["表格sch", "tablesch", "table sch"]),
      tableEN:  find(["表格en",  "tableen",  "table en"]),
    };
  ```
  改為：
  ```js
    const idx = {
      titleSch:   find(["標題sch", "标题sch"]),
      ruleSch:    find(["規則sch", "规则sch"]),
      titleEN:    find(["標題en", "标题en", "titleen"]),
      ruleEN:     find(["規則en", "规则en", "ruleen"]),
      tableSch:   find(["表格sch", "tablesch", "table sch"]),
      tableEN:    find(["表格en",  "tableen",  "table en"]),
      contentSch: find(["内容sch", "内容", "content"]),
      imageSch:   find(["示意图sch", "示意图", "image", "圖示", "图示"]),
    };
  ```

- [ ] **Step 2：整體替換 `findPrimaryRuleText` 函式**

  找到整個 `findPrimaryRuleText` 函式（約第 578–594 行）：
  ```js
  function findPrimaryRuleText(pageRows, ruleIdx) {
    const COL_CONTENT = 4;
    const COL_IMAGE   = 3;
    // 優先：内容Sch = "规则" 的行
    const ruleRow = pageRows.find(r => {
      const c = (r[COL_CONTENT] || "").trim();
      return c === '规则' || c === '規則';
    });
    if (ruleRow) return (ruleRow[ruleIdx] || "").trim();
    // 次選：内容Sch 為空 且 示意图Sch 不是 <bet> 格式
    const mainRow = pageRows.find(r => {
      const c   = (r[COL_CONTENT] || "").trim();
      const img = (r[COL_IMAGE]   || "").trim();
      return c === '' && !/^<\d+>$/.test(img);
    });
    return ((mainRow || pageRows[0])[ruleIdx] || "").trim();
  }
  ```
  完整替換為：
  ```js
  function findPrimaryRuleText(pageRows, ruleIdx, colIndex) {
    const COL_CONTENT = (colIndex && colIndex.contentSch >= 0) ? colIndex.contentSch : -1;
    const COL_IMAGE   = (colIndex && colIndex.imageSch   >= 0) ? colIndex.imageSch   : -1;

    // 優先：内容欄 = "规则" 或 "規則" 的行
    if (COL_CONTENT >= 0) {
      const ruleRow = pageRows.find(r => {
        const c = (r[COL_CONTENT] || "").trim();
        return c === '规则' || c === '規則';
      });
      if (ruleRow) return (ruleRow[ruleIdx] || "").trim();
    }

    // 次選：内容欄為空 且 圖示欄不是 <bet> 格式
    let mainRow;
    if (COL_CONTENT >= 0 || COL_IMAGE >= 0) {
      mainRow = pageRows.find(r => {
        const c   = COL_CONTENT >= 0 ? (r[COL_CONTENT] || "").trim() : '';
        const img = COL_IMAGE   >= 0 ? (r[COL_IMAGE]   || "").trim() : '';
        return c === '' && !/^<\d+>$/.test(img);
      });
    }

    return ((mainRow || pageRows[0])[ruleIdx] || "").trim();
  }
  ```

- [ ] **Step 3：整體替換 `detectTableInfo` 函式**

  找到整個 `detectTableInfo` 函式（約第 600–618 行）：
  ```js
  function detectTableInfo(pageRows, lang) {
    const COL_CONTENT = 4;
    const COL_IMAGE   = 3;
    const ruleIdx = lang === "sch" ? 5 : 8;

    // 賠率表：内容Sch = "赔率表"
    const paytableRows = pageRows.filter(r => (r[COL_CONTENT] || "").trim() === '赔率表');
    if (paytableRows.length > 0) return { type: 'paytable', rows: paytableRows, ruleIdx };

    // 投注符號表：示意图Sch = <數字>
    const betRows = pageRows.filter(r => /^<\d+>$/.test((r[COL_IMAGE] || "").trim()));
    if (betRows.length > 0) return { type: 'bet_symbols', rows: betRows, allRows: pageRows, ruleIdx };

    // 資料表：规则Sch 含有「數字 ~ 數字」格式，且多行
    const rangeRows = pageRows.filter(r => /\d+\s*[~～]\s*\d+/.test(r[ruleIdx] || ""));
    if (rangeRows.length > 1) return { type: 'data_table', rows: rangeRows, ruleIdx };

    return null;
  }
  ```
  完整替換為：
  ```js
  function detectTableInfo(pageRows, lang, colIndex) {
    const COL_CONTENT = (colIndex && colIndex.contentSch >= 0) ? colIndex.contentSch : -1;
    const COL_IMAGE   = (colIndex && colIndex.imageSch   >= 0) ? colIndex.imageSch   : -1;
    const ruleColIdx  = colIndex ? (lang === "sch" ? colIndex.ruleSch : colIndex.ruleEN) : -1;
    if (ruleColIdx < 0) return null;

    // 賠率表：内容欄 = "赔率表"
    if (COL_CONTENT >= 0) {
      const paytableRows = pageRows.filter(r => (r[COL_CONTENT] || "").trim() === '赔率表');
      if (paytableRows.length > 0) return { type: 'paytable', rows: paytableRows, ruleIdx: ruleColIdx };
    }

    // 投注符號表：圖示欄 = <數字>
    if (COL_IMAGE >= 0) {
      const betRows = pageRows.filter(r => /^<\d+>$/.test((r[COL_IMAGE] || "").trim()));
      if (betRows.length > 0) return { type: 'bet_symbols', rows: betRows, allRows: pageRows, ruleIdx: ruleColIdx };
    }

    // 資料表：規則欄含「數字 ~ 數字」格式，且多行
    const rangeRows = pageRows.filter(r => /\d+\s*[~～]\s*\d+/.test(r[ruleColIdx] || ""));
    if (rangeRows.length > 1) return { type: 'data_table', rows: rangeRows, ruleIdx: ruleColIdx };

    return null;
  }
  ```

- [ ] **Step 4：更新 `generateManual` 內的兩個呼叫點**

  **第一處**——找到（約第 129 行）：
  ```js
        const ruleText  = findPrimaryRuleText(pageRows, ruleIdx);
  ```
  改為：
  ```js
        const ruleText  = findPrimaryRuleText(pageRows, ruleIdx, colIndex);
  ```

  **第二處**——找到（約第 187 行）：
  ```js
            const tableInfo = detectTableInfo(pageRows, lang);
  ```
  改為：
  ```js
            const tableInfo = detectTableInfo(pageRows, lang, colIndex);
  ```

- [ ] **Step 5：Figma 內驗證**

  1. 使用**標準格式**試算表（有「内容Sch」「示意图Sch」欄位）→ 生成結果與修改前相同
  2. 使用**欄位位置不同**的試算表（例如把「内容」移到第 2 欄而非第 5 欄）→ 插件正確偵測欄位，不再用錯位置
  3. 使用**完全沒有** content/image 欄的試算表 → 插件仍然可以生成，只是跳過賠率表/投注符號偵測

- [ ] **Step 6：Commit**

  ```bash
  git add figma-plugin/code.js
  git commit -m "fix: 表格偵測改用動態欄位索引，移除硬編碼位置（Bug ④）"
  ```

---

### Task 5：修正表格生成錯誤被靜默吞掉（Bug ⑤，code.js）

**Files:**
- Modify: `figma-plugin/code.js`（`generateManual` 函式，約第 179–197 行）

- [ ] **Step 1：把 `catch (_) {}` 改為顯示警告訊息**

  找到表格生成的 try/catch 區塊結尾（約第 197 行）：
  ```js
        } catch (_) {}
  ```
  改為：
  ```js
        } catch (tableErr) {
          figma.ui.postMessage({ type: 'status', text: `⚠️ ${pageLabel} 表格跳過：${tableErr.message}` });
        }
  ```

- [ ] **Step 2：Figma 內驗證**

  1. 在試算表的「表格Sch」欄位填入格式無法解析的資料（例如只有一個字 `X`）
  2. 執行生成
  3. ✅ 預期：狀態欄顯示「⚠️ PAGE X 表格跳過：...」，其他頁面正常生成
  4. ❌ 之前行為：靜默略過，使用者不知道哪一頁的表格出問題

- [ ] **Step 3：Commit**

  ```bash
  git add figma-plugin/code.js
  git commit -m "fix: 表格生成失敗改為顯示警告而非靜默略過（Bug ⑤）"
  ```

---

### Task 6：版本號更新與最終驗收 Push

**Files:**
- Modify: `figma-plugin/ui.html`（版本號字串）

- [ ] **Step 1：更新版本號顯示**

  在 `figma-plugin/ui.html` 找到（約第 129 行）：
  ```html
    <div class="version">v3.1 — Google Sheets API</div>
  ```
  改為：
  ```html
    <div class="version">v3.10 — 穩定性修正</div>
  ```

- [ ] **Step 2：完整驗收清單（在 Figma 內執行）**

  - [ ] Bug ①：「修正圓點字型」執行後出現**綠色**狀態框
  - [ ] Bug ①：「指定字元換字型」執行後出現**綠色**或**紅色**狀態框（不再空白）
  - [ ] Bug ②：選取含 Instance 的節點執行換圖 → 正常節點被處理，Instance 跳過並顯示計數
  - [ ] Bug ③：選取混合字型節點執行換圖 → 正常執行，不 crash
  - [ ] Bug ④：標準試算表生成結果與修改前相同
  - [ ] Bug ⑤：試算表有格式錯誤的表格欄位時，狀態欄顯示 ⚠️ 警告而非靜默略過

- [ ] **Step 3：最終 Commit + Push**

  ```bash
  git add figma-plugin/ui.html
  git commit -m "chore: v3.10 版本號更新"
  git push
  ```
